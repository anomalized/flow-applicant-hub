export default function AcceptInvite() {
  return (
    <div
      className="flex items-center justify-center"
      style={{ minHeight: "100vh", backgroundColor: "var(--color-bg)", color: "var(--color-text)", fontFamily: "var(--font-base)" }}
    >
      <div>Invite accepted. Redirecting...</div>
    </div>
  );
}
