// Re-export the auto-generated Supabase client as the canonical `supabase` instance.
// Import as: `import supabase from "@/lib/supabase"` or `import { supabase } from "@/lib/supabase"`.
import { supabase } from "@/integrations/supabase/client";

export { supabase };
export default supabase;
